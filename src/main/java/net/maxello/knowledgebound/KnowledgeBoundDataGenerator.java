package net.maxello.knowledgebound;

/**
 * Placeholder for future data generation.
 *
 * So, here's the deal: right now we're not actually using Fabric's built-in 
 * data generator system. That's why this class is basically just sitting here 
 * twiddling its thumbs.
 *
 * If we ever decide down the road that we need to automatically generate JSON files 
 * for recipes, tags, or models (which saves a bunch of time over writing them by hand), 
 * we'll want to re-hook this up in the fabric.mod.json as a FabricDataGeneratorEntrypoint.
 * Until then, just leave it as a no-op!
 */
public class KnowledgeBoundDataGenerator {
    // Nothing to see here... yet!
}
